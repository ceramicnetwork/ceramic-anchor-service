name: CAS Auth tests

on:
  push: # all push events
  workflow_dispatch: # manually triggered

env:
  AWS_ACCOUNT_ID: non
  AWS_ACCESS_KEY_ID: non
  AWS_SECRET_ACCESS_KEY: non
  AWS_REGION: non

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout code
        uses: actions/checkout@v3
      -
        name: Start DynamoDB container
        run: docker run -d --rm -p 8000:8000 amazon/dynamodb-local
      -
        name: Setup pnpm
        run: |
          npm install -g pnpm
          pnpm install
        working-directory: ./auth
      -
        name: Run tests
        run: |
          export TESTING=true
          export REST_API_ID=
          export REST_API_ROOT_RESOURCE_ID=
          export CAS_PERMISSIONED_SECURITY_GROUP_IDS=
          env
          node -v
          pnpm -v
          serverless --version
          npx jest --version
          npx --version
          apt list --installed
           
          # Start Serverless Offline
          pnpm run start:testing &
          sleep 20
          pnpm run test -- integration
        working-directory: ./auth
